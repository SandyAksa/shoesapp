<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Colors</title>
</head>
<body>
    <%- include('partials/_navbar.ejs') %>
    
    <h1>List of Colors</h1>

    <table border=1>
        <tr>
            <th>Id</th>
            <th>Color</th>
            <th>Stock</th>
            <th>Action</th>
        </tr>

        <% for (let i = 0; i < colors.length; i++) { %>
            <tr>
                <td><%= colors[i].id %></td>
                <td><%= colors[i].name %></td>
                <td><%= colors[i].stock %></td>
                <td><a href="/colors/choose/<%= colors[i].id %>">Choose</a></td>
            </tr>
        <% } %>
    </table>
    <br>
    <a href="/shoes/add">NEXT</a><br>
</body>
</html>